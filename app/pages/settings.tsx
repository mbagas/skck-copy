import React from 'react';
import { UserLayout } from 'src/components/pageLayout';
import SettingsContent from 'src/components/userPage/SettingsContent';

const UserSettings = () => {
  return (
    <UserLayout>
      <SettingsContent />
    </UserLayout>
  );
};

export default UserSettings;
