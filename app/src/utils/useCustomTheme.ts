import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
  colors: {
    royalBlue: {
      100: '#4F72D9',
      200: '#184ADE',
    },
    textLogin: {
      SMA: '#F3EF24',
    },
  },
});

export default theme;
